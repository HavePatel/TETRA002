You are an expert invoice parser. Your task is to extract structured invoice data from the following raw OCR text.

Input OCR Text:
{ocr_text}

You must output ONLY a valid JSON object matching the following schema structure:
{{
  "invoice_number": null,
  "vendor": null,
  "gstin": null,
  "invoice_date": null,
  "subtotal": null,
  "gst": null,
  "total": null,
  "currency": null
}}

Rules:
1. Return ONLY valid raw JSON. Do NOT wrap the response in markdown code fences (like ```json ... ```) or add any pre-amble/post-amble text.
2. If a field is missing or cannot be determined, use null.
3. Extract currency only if a currency symbol or code is explicitly present in the document (e.g. $, USD, INR, £). Do not guess or assume a default currency.
4. Dates must be formatted strictly as YYYY-MM-DD. If the date format on the invoice is different (e.g., DD/MM/YYYY, DD-Mon-YYYY, or MM/DD/YYYY), convert it to YYYY-MM-DD format. If no date is found or it cannot be parsed, use null.
5. Do NOT invent/fabricate values. If a field is not present or cannot be determined, use null.
